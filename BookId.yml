info:
  name: BookId
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseUrl}}/books/:bookId"
  params:
    - name: bookId
      value: "3"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
