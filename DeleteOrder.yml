info:
  name: DeleteOrder
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{BaseUrl}}/orders/:orderId"
  params:
    - name: orderId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
