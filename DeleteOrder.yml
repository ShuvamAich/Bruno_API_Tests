info:
  name: DeleteOrder
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{BaseUrl}}/orders/:orderId"
  params:
    - name: orderId
      value: "{{OrderId}}"
      type: path
  auth:
    type: bearer
    token: "{{Authorization}}"

runtime:
  scripts:
    - type: tests
      code: bru.setNextRequest(null)
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
