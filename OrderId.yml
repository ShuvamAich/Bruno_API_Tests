info:
  name: OrderId
  type: http
  seq: 10

http:
  method: GET
  url: "{{BaseUrl}}/orders/:orderId"
  params:
    - name: orderId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
