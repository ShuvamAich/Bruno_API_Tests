info:
  name: Request API Key
  type: http
  seq: 16

http:
  method: POST
  url: "{{BaseUrl}}/api-clients"
  body:
    type: json
    data: |-
      {
         "clientName": "Bruno",
         "clientEmail": "shuvam@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
